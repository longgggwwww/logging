# Cấu hình Keycloak
# Điều chỉnh các giá trị này theo môi trường Keycloak của bạn

# URL của Keycloak server
KEYCLOAK_SERVER_URL=http://localhost:8080

# Realm name trong Keycloak
KEYCLOAK_REALM=master

# Public Client ID cho web-app (client public, không cần secret)
KEYCLOAK_PUBLIC_CLIENT_ID=web-app

# API Client ID cho api-service (bearer-only client)
KEYCLOAK_API_CLIENT_ID=api-service

# Client Secret chỉ cần cho confidential clients, không dùng cho public client
# KEYCLOAK_CLIENT_SECRET=your-client-secret-if-needed

# URL của API (api-service) - đảm bảo trỏ tới API đã được bảo vệ bởi Keycloak
# Ví dụ khi chạy docker-compose: http://localhost:3000
REACT_APP_API_BASE_URL=http://localhost:3000
