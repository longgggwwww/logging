# GitHub Actions CI/CD Flow Diagram

## Overall Architecture

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                           GITHUB REPOSITORY                                   │
│                                                                               │
│  Developer Push to main                                                       │
│         │                                                                     │
│         └─────► .github/workflows/deploy.yml                                 │
│                                                                               │
└───────────────────────────────┬──────────────────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│                        GITHUB ACTIONS RUNNER                                  │
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  JOB 1: Detect Changes                                              │    │
│  │  ────────────────────────                                           │    │
│  │                                                                      │    │
│  │  Check changed files:                                               │    │
│  │    services/api/**        → api: true                               │    │
│  │    services/processor/**  → processor: false                        │    │
│  │    services/realtime/**   → realtime: true                          │    │
│  │    web-app/**            → web-app: false                           │    │
│  │                                                                      │    │
│  │  Output: ["api", "realtime"]                                        │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                │                                              │
│                                ▼                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  JOB 2: Build (Matrix Strategy - Parallel)                          │    │
│  │  ───────────────────────────────────────                            │    │
│  │                                                                      │    │
│  │  ┌──────────────────┐              ┌──────────────────┐            │    │
│  │  │  Build: api      │              │  Build: realtime │            │    │
│  │  │  ──────────────  │              │  ──────────────  │            │    │
│  │  │  1. Checkout     │              │  1. Checkout     │            │    │
│  │  │  2. Setup Buildx │              │  2. Setup Buildx │            │    │
│  │  │  3. Load cache   │              │  3. Load cache   │            │    │
│  │  │  4. Build image  │              │  4. Build image  │            │    │
│  │  │  5. Export .tar  │              │  5. Export .tar  │            │    │
│  │  │  6. Upload       │              │  6. Upload       │            │    │
│  │  └──────────────────┘              └──────────────────┘            │    │
│  │         │                                    │                      │    │
│  │         └────────────┬───────────────────────┘                      │    │
│  └──────────────────────┼──────────────────────────────────────────────┘    │
│                         │                                                    │
│                         ▼                                                    │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  JOB 3: Deploy                                                       │    │
│  │  ──────────────                                                      │    │
│  │                                                                      │    │
│  │  1. Download artifacts                                              │    │
│  │     - api.tar                                                       │    │
│  │     - realtime.tar                                                  │    │
│  │                                                                      │    │
│  │  2. Setup SSH connection                                            │    │
│  │     - Load private key from secrets                                 │    │
│  │     - Add VPS to known hosts                                        │    │
│  │                                                                      │    │
│  │  3. Transfer files via rsync                                        │    │
│  │     ┌─────────────────────────────────────────┐                    │    │
│  │     │  rsync → VPS ~/log-monitoring/          │                    │    │
│  │     │    - docker-compose.yml                 │                    │    │
│  │     │    - api.tar                            │                    │    │
│  │     │    - realtime.tar                       │                    │    │
│  │     │    - services.json                      │                    │    │
│  │     └─────────────────────────────────────────┘                    │    │
│  │                                                                      │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘
                                │
                                │ SSH Connection
                                ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│                              VPS SERVER                                       │
│                         ~/log-monitoring/                                     │
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  Deployment Script Execution                                         │    │
│  │  ───────────────────────────                                        │    │
│  │                                                                      │    │
│  │  1. Load Docker Images                                              │    │
│  │     ┌────────────────────────────────────┐                         │    │
│  │     │  docker load -i api.tar            │                         │    │
│  │     │  → Image: api:latest               │                         │    │
│  │     │                                     │                         │    │
│  │     │  docker load -i realtime.tar       │                         │    │
│  │     │  → Image: realtime:latest          │                         │    │
│  │     └────────────────────────────────────┘                         │    │
│  │                                                                      │    │
│  │  2. Restart Changed Services Only                                   │    │
│  │     ┌────────────────────────────────────┐                         │    │
│  │     │  docker compose up -d --no-deps \  │                         │    │
│  │     │    api realtime                    │                         │    │
│  │     │                                     │                         │    │
│  │     │  ✅ api: recreated                 │                         │    │
│  │     │  ✅ realtime: recreated            │                         │    │
│  │     │  ⏸️  processor: unchanged          │                         │    │
│  │     │  ⏸️  discord-bot: unchanged        │                         │    │
│  │     │  ⏸️  fcm: unchanged                │                         │    │
│  │     │  ⏸️  web-app: unchanged            │                         │    │
│  │     └────────────────────────────────────┘                         │    │
│  │                                                                      │    │
│  │  3. Cleanup                                                         │    │
│  │     - Remove deployment artifacts                                   │    │
│  │     - Prune old Docker images                                       │    │
│  │                                                                      │    │
│  │  4. Verify Deployment                                               │    │
│  │     - docker compose ps                                             │    │
│  │     - Check service health                                          │    │
│  │                                                                      │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                               │
│  Running Services:                                                            │
│  ┌────────────┬──────────┬─────────────────────┬────────────────────┐       │
│  │  Service   │  Status  │  Image              │  Ports              │       │
│  ├────────────┼──────────┼─────────────────────┼────────────────────┤       │
│  │  api       │  Running │  api:latest (new)   │  0.0.0.0:3000      │       │
│  │  realtime  │  Running │  realtime:latest    │  0.0.0.0:5000      │       │
│  │  processor │  Running │  processor:old      │  -                 │       │
│  │  web-app   │  Running │  web-app:old        │  0.0.0.0:8000      │       │
│  │  kafka-1   │  Running │  apache/kafka:3.7.0 │  19092             │       │
│  │  postgres  │  Running │  postgres:15        │  -                 │       │
│  │  mongodb   │  Running │  mongo:7.0          │  27017             │       │
│  └────────────┴──────────┴─────────────────────┴────────────────────┘       │
│                                                                               │
└──────────────────────────────────────────────────────────────────────────────┘
```

## Change Detection Logic

```
┌─────────────────────────────────────────────────────────────────┐
│  Git Diff Analysis                                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Changed Files                      Detected Service            │
│  ─────────────                      ────────────────            │
│                                                                 │
│  services/api/src/routes/logs.ts    → api ✓                    │
│  services/api/src/app.ts            → api ✓                    │
│  services/realtime/src/server.ts    → realtime ✓               │
│  web-app/src/pages/Dashboard.tsx    → web-app ✓                │
│  docs/README.md                     → (no service)              │
│  docker-compose.yml                 → (all services)            │
│                                                                 │
│  Result: ["api", "realtime", "web-app"]                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Timeline Example

```
Single Service Change (api only):
─────────────────────────────────────

0:00  ┬ Push to main
      │
0:15  ┼ Detect changes      [✓ api detected]
      │
0:30  ┼ Start build         [Building api...]
      │
3:30  ┼ Build complete      [Uploading artifact...]
      │
4:00  ┼ Start deployment    [Downloading artifact...]
      │
4:30  ┼ Transfer to VPS     [Uploading via rsync...]
      │
5:00  ┼ Load & restart      [docker compose up -d api]
      │
5:30  ┴ Complete ✅         [api running]


Multiple Services (api, realtime, web-app):
────────────────────────────────────────────

0:00  ┬ Push to main
      │
0:15  ┼ Detect changes      [✓ api, realtime, web-app]
      │
0:30  ┼ Start parallel build
      │   ┌─ Building api...
      │   ├─ Building realtime...
      │   └─ Building web-app...
      │
5:00  ┼ All builds complete [Uploading artifacts...]
      │
5:30  ┼ Start deployment    [Downloading artifacts...]
      │
6:00  ┼ Transfer to VPS     [Uploading via rsync...]
      │
7:00  ┼ Load & restart      [docker compose up -d api realtime web-app]
      │
8:00  ┴ Complete ✅         [All services running]
```

## Build Cache Strategy

```
First Build (No Cache):
───────────────────────

┌─────────────────────────┐
│  1. Pull base image     │  30s   (FROM node:20-alpine)
├─────────────────────────┤
│  2. Install deps        │  180s  (npm install)
├─────────────────────────┤
│  3. Copy source         │  5s    (COPY . .)
├─────────────────────────┤
│  4. Build TypeScript    │  60s   (npm run build)
├─────────────────────────┤
│  5. Create image        │  10s   (Final layers)
└─────────────────────────┘
Total: ~285s (~5 min)


Subsequent Builds (With Cache):
────────────────────────────────

┌─────────────────────────┐
│  1. Pull base image     │  5s    ← Cached
├─────────────────────────┤
│  2. Install deps        │  10s   ← Cached (package.json unchanged)
├─────────────────────────┤
│  3. Copy source         │  5s    ← Changed files only
├─────────────────────────┤
│  4. Build TypeScript    │  60s   ← Rebuild needed
├─────────────────────────┤
│  5. Create image        │  5s    ← Fast
└─────────────────────────┘
Total: ~85s (~1.5 min)

Cache Savings: ~70%! 🚀
```

## Network Transfer Optimization

```
Without Optimization (scp):
───────────────────────────

Files to transfer:
- api.tar              (350 MB)
- realtime.tar         (320 MB)
- processor.tar        (380 MB)
- docker-compose.yml   (5 KB)
- init-scripts/        (10 KB)

Total: ~1050 MB
Time: ~5-7 minutes (on typical VPS connection)


With Optimization (rsync + selective transfer):
────────────────────────────────────────────────

Only transfer changed services:
- api.tar              (350 MB)  ← Only if changed
- realtime.tar         (320 MB)  ← Only if changed
- docker-compose.yml   (5 KB)
- services.json        (1 KB)

Example (api + realtime changed):
Total: ~670 MB
Time: ~3-4 minutes

Savings: ~40% time! ⚡
```

## Comparison: Old vs New Deployment

```
Old Method (Manual Deploy All):
────────────────────────────────

1. SSH to VPS
2. git pull
3. docker compose build        ← Build ALL services (15-20 min)
4. docker compose up -d        ← Restart ALL services
5. Check logs manually

Total: ~20-25 minutes
Downtime: All services restart
Manual: Yes
Rollback: Manual


New Method (GitHub Actions):
─────────────────────────────

1. git push origin main        ← Automatic from here
2. Detect changes              ← Only changed services
3. Build in parallel           ← GitHub's powerful servers
4. Deploy smart                ← Only restart changed services
5. Auto verify

Total: ~3-8 minutes (depending on changes)
Downtime: Only changed services (seconds)
Manual: No
Rollback: Re-run previous workflow
```

---

**Legend:**
- ✓ = Detected/Changed
- ✅ = Success
- ⏸️ = Unchanged/Skipped
- → = Results in
- ← = Because of
